# 会员与系统管理员使用独立身份体系

手机号会员只登录移动应用，系统管理员只使用独立管理员用户名和密码登录管理后台；两者采用不同数据表、登录接口和会话类型，不允许身份复用。移动应用采用短期访问令牌和可轮换刷新令牌且一期只保留一个有效设备会话，管理后台采用安全的 HttpOnly Cookie；会员或管理员修改密码、会员被停用以及新设备登录等事件会撤销相关旧会话。这一选择增加了两套认证入口的实现成本，但避免高权限管理员凭据进入面向公众的会员认证边界，并减少跨客户端权限混淆。
