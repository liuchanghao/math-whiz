# 答题轮次由服务端持有并在完成后统一揭示结果

服务端创建并持有会员唯一一个进行中的答题轮次，固定保存本轮抽取的 10 道题及顺序，并在会员逐题提交时持久化答案；移动应用中途退出后恢复该轮次。会员可以主动放弃进行中轮次，持续 7 天未完成的轮次也自动转为已放弃；已放弃轮次不评分、不生成报告、不进入错题本且不给奖品。服务端在前 9 次提交时不返回正确答案或即时对错，10 道题全部完成后才统一评分并生成包含总分、错题、正确答案和解析的答题报告。这一选择增加了服务端状态和恢复逻辑，但避免客户端重新抽题、篡改轮次或提前获得答案，并保证恢复的一致性。
